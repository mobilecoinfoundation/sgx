[package]
name = "mc-sgx-dcap-ql-sys-types"
version = "0.5.1-beta.0"
authors = ["MobileCoin"]
categories = ["external-ffi-bindings", "hardware-support", "no-std"]
description = "FFI type definitions for the `sgx_dcap_ql` library."
edition = "2021"
keywords = ["ffi", "sgx", "no-std"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/mobilecoinfoundation/sgx"
rust-version = "1.62.1"

[lib]
doctest = false

[build-dependencies]
bindgen = "0.64.0"
cargo-emit = "0.2.1"
mc-sgx-core-build = { path = "../../../../core/build", version = "=0.5.1-beta.0" }
