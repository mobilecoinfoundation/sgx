[workspace]
resolver = "2"
members = [
    "core/sys/types",
    "core/build",
    "crypto/sys/types",
    "crypto/sys",
    "panic/abort",
    "quote_verify",
    "service/sys",
    "service/sys/types",
    "trts/sys",
    "urts",
    "urts/sys",
    "urts/sys/types",
]
exclude = [
    "test_enclave",
]

[profile.dev]
opt-level = 0
rpath = true
lto = true

[profile.release]
opt-level = 3
rpath = false
lto = true
debug-assertions = false
overflow-checks = false

# Skip the need for LD_LIBRARY_PATH in `cargo test`
[profile.test]
rpath = true

